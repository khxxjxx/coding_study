// import React, { useState } from 'react';
// import './UI.css';

// const UI = props => {
//   return (
//     props.addUser != '' && (
//       <div className="ui">
//         {props.addUser.map((user, id) => {
//           return <p key={id}> {`${user.name} (${user.age} years old)`} </p>;
//         })}
//       </div>
//     )
//   );
// };

// export default UI;
